# PID Controller

Classic proportional-integral-derivative control examples for training dynamics optimization.

The PID controller work is co-located with the Q-learning optimizer in `OPTIMIZERS/q-controller/`:
- `pidexample.py` — PID controller example for training dynamics
- `qcontroller.py` — HAKMEM Q-learning controller (the primary WuBu optimizer)

# `tests/` — Test Harness

Regression testing for the bytropix inference engine.

| File | Purpose |
|------|---------|
| `run.sh` | Shell test harness runner |
| `test_api.sh` | API server sandbox tests |
| `golden/` | Golden reference outputs for regression |

## Usage
```bash
bash tests/run.sh
```
